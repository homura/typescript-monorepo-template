{
  "name": "@foo/lib-a",
  "version": "0.0.1",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "npm run build:ts",
    "build:ts": "tsc -b .",
    "clean": "rimraf lib",
    "test": "jest",
    "test:watch": "jest --watch",
    "coverage": "jest --coverage",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "precommit": "lint-staged"
  }
}
